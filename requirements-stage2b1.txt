# Stage 2B-1 environment / reward-PBRS integration dependencies.
# Python used: 3.14.6 in dedicated .venv_stage2b1
# (Local Python 3.12 install has a corrupted stdlib json module; not used.)
# highway-env 1.12.0 declares Python 3.14 support (since 1.11).

numpy==2.3.2
gymnasium==1.2.0
highway-env==1.12.0
pytest==8.4.1
PyYAML==6.0.2
# transitive pins recorded at install time for reproducibility notes:
# pygame-ce==2.5.7, matplotlib==3.11.1, pandas==2.3.3, cloudpickle==3.1.2
