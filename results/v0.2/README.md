# v0.2 Results (Random Policy + Global Fairness Metrics)

**Restored from Git tag `v0.2`** (commit `344bed5`).

## Files in this folder

- `random_baseline.csv`, `random_rawlsian.csv`, `random_summary.csv`
- `random_comparison_*.png` (total reward, min experience, Gini, collision)

No DQN training in v0.2 — random policy only.
